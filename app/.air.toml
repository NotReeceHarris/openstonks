root = "."
tmp_dir = "/tmp/air"

[build]
  cmd = "go build -o /tmp/air/openstonks ./cmd/openstonks"
  bin = "/tmp/air/openstonks"
  include_ext = ["go"]
  exclude_dir = ["vendor"]
  delay = 500

[log]
  time = true
